# 🔐 权限问题修复说明

## 问题描述

**用户报告的错误**:
```
[错误] 复制文件失败：test1.txt - "test1.txt" couldn't be copied because you don't have permission to access "test_target".
```

虽然在添加配置时使用了 `NSOpenPanel` 选择文件夹，并创建了 security-scoped bookmarks，但文件复制时仍然报告权限错误。

---

## 根本原因分析

### 问题 1: 权限引用未保存

**错误代码** (修复前):
```swift
// ❌ 错误：调用后立即丢弃 URL 引用
if let sourceBookmark = configuration.sourceBookmarkData {
    do {
        let sourceURL = try fileManager.resolveSecurityScopedBookmark(sourceBookmark)
        _ = sourceURL.startAccessingSecurityScopedResource()  // 返回值被忽略
    } catch {
        logManager.warning("恢复源文件夹权限失败")
    }
}
// sourceURL 超出作用域，权限被自动释放！
```

**问题**:
- `startAccessingSecurityScopedResource()` 被调用了
- 但 `sourceURL` 变量在 `do` 块结束后超出作用域
- Swift 自动释放了 URL 对象，导致权限失效
- 后续的文件操作失去了权限

### 问题 2: 权限生命周期太短

**错误流程**:
```
1. validatePaths() 中调用 startAccessingSecurityScopedResource()
2. validatePaths() 返回，URL 被释放
3. 权限失效 ❌
4. scanFiles() 执行 - 可能成功（只读操作）
5. performSync() 执行 - 失败（写操作需要权限）❌
```

### 问题 3: 异步操作中权限丢失

在异步操作（`async`/`await`）中，如果不保持对 URL 的强引用，权限可能在执行过程中被释放。

---

## 解决方案

### 核心原理

**Security-Scoped Resource 的正确使用方式**:

1. **调用 `startAccessingSecurityScopedResource()`**
   - 返回 `Bool` 表示是否成功
   - 必须保存 URL 引用

2. **保持 URL 引用直到操作完成**
   - URL 对象必须在整个操作期间存活
   - 使用强引用（不能是 `weak` 或临时变量）

3. **操作完成后调用 `stopAccessingSecurityScopedResource()`**
   - 释放系统资源
   - 使用 `defer` 确保总是被调用

### 修复后的代码

```swift
@MainActor
func startSync(configuration: SyncConfiguration) async -> SyncResult {
    // ...

    // ✅ 1. 声明变量保存 security-scoped URLs
    var sourceSecurityScopedURL: URL?
    var targetSecurityScopedURL: URL?

    // ✅ 2. 使用 defer 确保权限被正确释放
    defer {
        sourceSecurityScopedURL?.stopAccessingSecurityScopedResource()
        targetSecurityScopedURL?.stopAccessingSecurityScopedResource()
    }

    do {
        // ✅ 3. 恢复源文件夹权限
        if let sourceBookmark = configuration.sourceBookmarkData {
            do {
                let url = try fileManager.resolveSecurityScopedBookmark(sourceBookmark)
                if url.startAccessingSecurityScopedResource() {
                    sourceSecurityScopedURL = url  // ✅ 保存引用
                    logManager.debug("已恢复源文件夹访问权限: \(url.path)")
                } else {
                    logManager.warning("无法访问源文件夹: \(url.path)")
                }
            } catch {
                logManager.warning("恢复源文件夹权限失败: \(error.localizedDescription)")
            }
        }

        // ✅ 4. 恢复目标文件夹权限
        if let targetBookmark = configuration.targetBookmarkData {
            do {
                let url = try fileManager.resolveSecurityScopedBookmark(targetBookmark)
                if url.startAccessingSecurityScopedResource() {
                    targetSecurityScopedURL = url  // ✅ 保存引用
                    logManager.debug("已恢复目标文件夹访问权限: \(url.path)")
                } else {
                    logManager.warning("无法访问目标文件夹: \(url.path)")
                }
            } catch {
                logManager.warning("恢复目标文件夹权限失败: \(error.localizedDescription)")
            }
        }

        // ✅ 5. 现在可以安全地访问文件
        try validatePaths(configuration: configuration)
        let (sourceFiles, targetFiles) = try await scanFiles(configuration: configuration)
        let changes = analyzeChanges(...)
        let result = try await performSync(...)  // ✅ 权限在整个过程中有效

        return result

    } catch {
        // ...
    }

    // ✅ 6. defer 块确保函数退出时释放权限
}
```

---

## 技术要点

### 1. `startAccessingSecurityScopedResource()` 返回值

```swift
let url = try URL(resolvingBookmarkData: bookmarkData, ...)

// ⚠️ 检查返回值！
if url.startAccessingSecurityScopedResource() {
    // ✅ 成功获取权限
    // 保存 url 引用
} else {
    // ❌ 获取权限失败
    // 可能原因：bookmark 过期、用户撤销权限等
}
```

### 2. 使用 `defer` 管理资源

```swift
func doSomething() {
    var resource: Resource?

    defer {
        // ✅ 无论成功还是失败，总是执行
        resource?.release()
    }

    do {
        resource = acquireResource()
        // ... 使用 resource
    } catch {
        // 即使抛出异常，defer 块也会执行
    }
}
```

### 3. 权限作用域

```swift
// ❌ 错误：作用域太小
func validatePaths() {
    let url = ...
    url.startAccessingSecurityScopedResource()
}  // url 被释放，权限失效

func copyFiles() {
    // ❌ 没有权限了！
}

// ✅ 正确：作用域覆盖所有操作
func performSync() {
    let url = ...
    if url.startAccessingSecurityScopedResource() {
        defer { url.stopAccessingSecurityScopedResource() }

        validatePaths()
        copyFiles()  // ✅ 权限仍然有效
    }
}
```

### 4. 异步操作中的权限

```swift
func syncFiles() async {
    var url: URL?

    defer {
        url?.stopAccessingSecurityScopedResource()
    }

    if let bookmark = savedBookmark {
        url = try URL(resolvingBookmarkData: bookmark, ...)
        url?.startAccessingSecurityScopedResource()
    }

    // ✅ 异步操作也能保持权限
    await scanFiles()   // url 引用仍然有效
    await copyFiles()   // url 引用仍然有效
}
```

---

## 日志输出对比

### 修复前（权限丢失）

```
[调试] 扫描: 配置验证通过: 源=..., 目标=...
[调试] 扫描: 路径验证通过
[调试] 扫描: 扫描到 3 个文件 (位置: 源)
[调试] 扫描: 扫描到 0 个文件 (位置: 目标)
[信息] 扫描: 扫描完成: 源(3个文件) 目标(0个文件)
[信息] 复制: 开始执行同步操作: 总共 3 个操作
[信息] 复制: 开始复制新文件: 3 个
[错误] 错误: 复制文件失败：test1.txt - couldn't be copied because you don't have permission  ❌
[错误] 错误: 复制文件失败：test2.txt - couldn't be copied because you don't have permission  ❌
[错误] 错误: 复制文件失败：test3.txt - couldn't be copied because you don't have permission  ❌
```

### 修复后（权限正常）

```
[调试] 扫描: 配置验证通过: 源=..., 目标=...
[调试] 扫描: 已恢复源文件夹访问权限: /Users/Ning/Desktop/test_source  ✅
[调试] 扫描: 已恢复目标文件夹访问权限: /Users/Ning/Desktop/test_target  ✅
[调试] 扫描: 路径验证通过
[调试] 扫描: 扫描到 3 个文件 (位置: 源)
[调试] 扫描: 扫描到 0 个文件 (位置: 目标)
[信息] 扫描: 扫描完成: 源(3个文件) 目标(0个文件)
[信息] 复制: 开始执行同步操作: 总共 3 个操作
[信息] 复制: 开始复制新文件: 3 个
[调试] 复制: 复制文件: test1.txt  ✅
[调试] 复制: 复制文件: test2.txt  ✅
[调试] 复制: 复制文件: test3.txt  ✅
[信息] 复制: 同步完成: 添加了 3 个文件  ✅
```

---

## 如何更新和测试

### 步骤 1: 拉取最新代码

```bash
cd /Users/Ning/Github/nings/macfoldersync
git pull origin claude/implement-project-modules-012sRvPv9iPjabTbBa1qLYRB
```

**预期输出**:
```
From http://...
   31f7ad4..f9e3bb9  claude/implement-project-modules-012sRvPv9iPjabTbBa1qLYRB -> origin/claude/implement-project-modules-012sRvPv9iPjabTbBa1qLYRB
Updating 31f7ad4..f9e3bb9
Fast-forward
 FolderSyncPro/FolderSyncPro/Services/SyncEngine.swift | 78 +++++++++++++++++++++----
 1 file changed, 57 insertions(+), 21 deletions(-)
```

### 步骤 2: 清理并重新构建

```bash
cd /Users/Ning/Github/nings/Xcode/MacOS/FolderSyncPro
rm -rf ~/Library/Developer/Xcode/DerivedData/FolderSyncPro-*
killall Xcode
open FolderSyncPro.xcodeproj
```

在 Xcode 中：
- Clean: `⌘ + Shift + K`
- Build: `⌘ + B`
- Run: `⌘ + R`

### 步骤 3: 删除旧配置（重要！）

**必须删除并重新创建配置**，因为：
- 旧配置可能没有保存 bookmark data
- 或 bookmark 可能已过期
- 需要重新授权

在应用中：
1. 删除所有现有配置
2. 添加新配置

### 步骤 4: 重新测试

```bash
# 确保测试文件存在
ls -la ~/Desktop/test_source
# 应该看到 test1.txt, test2.txt, test3.txt

# 清空目标文件夹
rm -rf ~/Desktop/test_target/*
```

在应用中：
1. 添加新配置：
   - **源**: 使用"选择..."按钮 → 选择 `桌面/test_source`
   - **目标**: 使用"选择..."按钮 → 选择 `桌面/test_target`
   - 同步模式: 源到目标
   - 激活: ✓
2. 保存配置
3. 切换到"日志"标签页
4. 点击"开始同步"

### 步骤 5: 验证结果

**查看日志**，应该看到：
```
[调试] 扫描: 已恢复源文件夹访问权限: /Users/Ning/Desktop/test_source
[调试] 扫描: 已恢复目标文件夹访问权限: /Users/Ning/Desktop/test_target
...
[调试] 复制: 复制文件: test1.txt
[调试] 复制: 复制文件: test2.txt
[调试] 复制: 复制文件: test3.txt
[信息] 复制: 同步完成: 添加了 3 个文件
```

**检查文件**:
```bash
ls -la ~/Desktop/test_target
# 应该看到:
# test1.txt
# test2.txt
# test3.txt
```

---

## 常见问题

### Q1: 为什么需要删除旧配置？

A: 旧配置可能：
- 没有保存 bookmark data（早期版本的 bug）
- Bookmark 已过期
- 路径不匹配

重新创建配置会触发新的权限请求并保存新的 bookmark。

### Q2: 日志显示"恢复...权限失败"怎么办？

A: 检查：
1. 配置是否使用"选择..."按钮创建（不是手动输入路径）
2. Bookmark data 是否已保存（检查配置详情）
3. 文件夹是否仍然存在
4. 尝试删除并重新创建配置

### Q3: 为什么有时候扫描成功，复制失败？

A:
- 扫描（读操作）的权限要求较低
- 复制（写操作）需要完整的读写权限
- 这就是为什么之前扫描成功但复制失败的原因

---

## 相关资料

- **Apple 文档**: [App Sandbox](https://developer.apple.com/documentation/security/app_sandbox)
- **Apple 文档**: [Security-Scoped Bookmarks](https://developer.apple.com/documentation/foundation/url/bookmarkcreationoptions/2143697-withsecurityscope)
- **修复提交**: `f9e3bb9 - fix: 正确管理 security-scoped resource 权限生命周期`

---

**最后更新**: 2025-11-22
**提交**: `f9e3bb9`
**状态**: ✅ 权限问题已修复
