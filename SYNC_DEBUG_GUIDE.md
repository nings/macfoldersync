# 🐛 同步功能调试指南

## 问题：点击"开始同步"后文件没有同步

### 🔍 可能的原因

#### 1. **文件夹权限问题**（最常见）

macOS 应用沙箱需要显式授予文件夹访问权限。

**检查方法**:
1. 打开应用
2. 创建新的同步配置
3. 点击选择源文件夹和目标文件夹
4. 系统应该弹出权限请求对话框

**如果没有弹出权限对话框**:
- 应用没有正确请求权限
- 需要使用 `NSOpenPanel` 来选择文件夹

#### 2. **路径配置问题**

**检查**:
- 源路径和目标路径是否正确配置
- 路径是否真实存在
- 路径中是否有特殊字符

#### 3. **没有可同步的文件**

**检查**:
- 源文件夹中是否有文件
- 文件是否被排除规则过滤了（默认排除 `.DS_Store`, `.git/*` 等）

### 🔧 快速诊断步骤

#### 步骤 1: 查看日志

1. 在同步配置界面，切换到"日志"标签页
2. 点击"开始同步"
3. 查看是否有错误信息

**预期日志**:
```
[信息] 开始同步: 配置名称
[信息] 开始扫描文件...
[信息] 扫描完成: 源(X个文件) 目标(Y个文件)
[信息] 同步完成: ...
```

**如果看到错误**:
- 记录错误信息
- 可能是权限、路径或其他问题

#### 步骤 2: 创建测试配置

1. 创建两个简单的测试文件夹:
   ```bash
   mkdir ~/Desktop/test_source
   mkdir ~/Desktop/test_target
   echo "hello" > ~/Desktop/test_source/test.txt
   ```

2. 在应用中创建新配置:
   - 名称: "测试同步"
   - 源: `~/Desktop/test_source`
   - 目标: `~/Desktop/test_target`
   - 同步模式: "源到目标"

3. 点击"开始同步"

4. 检查 `~/Desktop/test_target/` 是否出现 `test.txt`

#### 步骤 3: 检查文件选择器

当前的实现可能需要用户使用文件选择器来选择文件夹（以获得权限）。

**检查 MainWindow.swift 中是否有文件选择器**:
```swift
// 应该使用 NSOpenPanel
let openPanel = NSOpenPanel()
openPanel.canChooseFiles = false
openPanel.canChooseDirectories = true
openPanel.allowsMultipleSelection = false
```

### 🛠️ 临时解决方案

#### 方案 1: 禁用沙箱（仅用于测试）

编辑 `FolderSyncPro.entitlements`:
```xml
<key>com.apple.security.app-sandbox</key>
<false/>  <!-- 改为 false -->
```

**警告**: 这会移除沙箱保护，仅用于测试！发布时必须启用。

#### 方案 2: 添加调试日志

我可以为您添加更多调试日志来诊断问题。

### 📊 检查清单

- [ ] 源文件夹路径正确且存在
- [ ] 目标文件夹路径正确且存在
- [ ] 源文件夹中有文件
- [ ] 应用有权限访问这两个文件夹
- [ ] 查看日志标签页是否有错误
- [ ] 同步配置已保存
- [ ] 配置标记为"已激活"

### 🔍 进一步调试

如果以上都检查过了，问题可能是：

1. **SwiftData 存储问题** - 配置没有正确保存
2. **异步任务问题** - Task 没有正确执行
3. **UI 状态问题** - SyncEngine 实例不正确

---

## 🚀 需要我帮您添加什么？

### 选项 1: 添加详细的调试日志
我可以在关键位置添加日志输出，帮助诊断问题。

### 选项 2: 添加错误提示弹窗
同步失败时显示明确的错误信息。

### 选项 3: 添加权限检查
在开始同步前检查文件夹权限。

### 选项 4: 添加文件选择器
如果还没有，添加使用 NSOpenPanel 选择文件夹。

---

## ❓ 请告诉我

1. **您看到任何日志了吗？** 在"日志"标签页
2. **您是如何设置源和目标路径的？** 手动输入还是选择文件夹？
3. **源文件夹里有文件吗？**
4. **有任何错误提示吗？**

请告诉我上面的信息，我会针对性地帮您修复！
